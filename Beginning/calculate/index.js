const {calculateMultiply } = require("./calculateMultiply");
const { x , calculateSum } = require("./calculateSum");

module.exports = { calculateMultiply, calculateSum , x};

// if project have lot of import and export. just import all export in one file
// like index.js and from there export so you have to just import from index.js
